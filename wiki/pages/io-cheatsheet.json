{
  "title": "I/O cheatsheet",
  "layout": "canvas",
  "cards": [
    {
      "id": "title",
      "type": "text",
      "x": 40,
      "y": 40,
      "w": 620,
      "h": 60,
      "z": 0,
      "format": "markdown",
      "md": "# I/O cheatsheet\n\nThe whole of entity I/O on one screen. The long version is [[io-concepts]]."
    },
    {
      "id": "f1",
      "type": "text",
      "x": 40,
      "y": 190,
      "w": 250,
      "h": 140,
      "z": 1,
      "format": "markdown",
      "md": "### 1. Output fires\n\nThe entity emits a signal, like `pressed(activator)`."
    },
    {
      "id": "f2",
      "type": "text",
      "x": 370,
      "y": 190,
      "w": 250,
      "h": 140,
      "z": 2,
      "format": "markdown",
      "md": "### 2. Times, then delay\n\nThe fire is counted against *Times*, then waits *Delay* seconds."
    },
    {
      "id": "f3",
      "type": "text",
      "x": 700,
      "y": 190,
      "w": 250,
      "h": 140,
      "z": 3,
      "format": "markdown",
      "md": "### 3. Resolve target\n\nThe target selector becomes a list of nodes. None found means nothing happens."
    },
    {
      "id": "f4",
      "type": "text",
      "x": 1030,
      "y": 190,
      "w": 250,
      "h": 140,
      "z": 4,
      "format": "markdown",
      "md": "### 4. Call input\n\nThe input method is called on each node with the parameter."
    },
    {
      "id": "targets",
      "type": "text",
      "x": 40,
      "y": 370,
      "w": 560,
      "h": 60,
      "z": 5,
      "format": "markdown",
      "md": "## Targets\n\n| Write | Reaches |\n| --- | --- |\n| `main_door` | That targetname, in the same map |\n| `door_*` | Targetnames starting with `door_` |\n| `@enemies` | The Godot group, whole tree |\n| `!player` | First node in group `player` |\n| `!activator` | Who started the chain (`!caller` too) |\n| `!self` | The entity with the output |\n| `/root/Game` | An absolute node path |"
    },
    {
      "id": "params",
      "type": "text",
      "x": 630,
      "y": 370,
      "w": 650,
      "h": 60,
      "z": 6,
      "format": "markdown",
      "md": "## Parameters\n\nA plain value is tried as int, float, bool, then `x y z` as a `Vector3`, else it stays a string. `[a, b]` in JSON spreads into several arguments.\n\nPlaceholders `$activator`, `$self`, `$position` and `$caller_name` are filled in on the **receiving** entity.\n\nNo parameter: the activator goes into the first object argument, wherever it is, and any other values the output passed along fill the rest in order (`false` forwards too, so it can end up as `0`)."
    },
    {
      "id": "activator",
      "type": "text",
      "x": 630,
      "y": 640,
      "w": 650,
      "h": 60,
      "z": 7,
      "format": "markdown",
      "md": "## Who keeps the activator\n\nPass it on\n: `triggered`, `pressed`, `entered`, `exited`, `hurt`, `teleported`, `pushed`\nReplace it\n: `arrived(corner)`, `spawned(node)`, `reached(train)`\nDrop it\n: `opened`, `timer`, `step_N`, `on_true`, `broken`, `switched`, `map_spawn`\n\nLost it? Target `!player` instead."
    },
    {
      "id": "fallbacks",
      "type": "text",
      "x": 40,
      "y": 776,
      "w": 560,
      "h": 60,
      "z": 8,
      "format": "markdown",
      "md": "## Inputs any node has\n\n`kill`, `show`, `hide`, `enable`, `disable`, `toggle`, used when no method of that name exists. An unknown input with a parameter sets the property of that name instead."
    },
    {
      "id": "script",
      "type": "text",
      "x": 40,
      "y": 944,
      "w": 560,
      "h": 60,
      "z": 9,
      "format": "markdown",
      "md": "## logic_script scope\n\n`this`, `activator`, `parameter`, `io` and `tree`. Indent with tabs."
    },
    {
      "id": "scriptcode",
      "type": "code",
      "x": 40,
      "y": 1056,
      "w": 560,
      "h": 60,
      "z": 10,
      "lang": "gdscript",
      "code": "var p = io.find_targets(this, \"!player\", null)\nif not p.is_empty():\n\tp[0].take_damage(25, this)"
    },
    {
      "id": "debug",
      "type": "text",
      "x": 630,
      "y": 880,
      "w": 650,
      "h": 60,
      "z": 11,
      "format": "markdown",
      "md": "## Nothing happened?\n\n> [!TIP] Check in this order\n> The Issues panel for unknown targetnames, the Logic panel to fire the chain, F3 in game for what actually fired, and `GodotTrenchIO.events()` to log every output."
    }
  ],
  "edges": [
    {
      "id": "flow1",
      "from": "f1",
      "to": "f2"
    },
    {
      "id": "flow2",
      "from": "f2",
      "to": "f3"
    },
    {
      "id": "flow3",
      "from": "f3",
      "to": "f4"
    }
  ]
}
